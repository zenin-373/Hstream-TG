wzgram[fast]>=2.0.0
python-dotenv>=1.0.0
yt-dlp>=2024.1.0
requests>=2.28.0
tqdm>=4.65.0
hanime-plugin>=2025.10.1

# System tools (not installable via pip):
#   sudo apt-get install -y aria2 ffmpeg
#   brew install aria2 ffmpeg
# On Colab: !apt-get install -y -qq aria2 ffmpeg
# Dockerfile + ensure_dependencies() install these on Debian/Ubuntu.
